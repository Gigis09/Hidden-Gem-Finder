# Hidden-Gem-Finder
# Used as a database to search for movies, insert movies, delete movies, update times, join query, aggregate query and utilizes a hidden gems feature to identify movies with low amounts of ratings yet high ratings. 
