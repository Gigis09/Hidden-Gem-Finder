# Hidden-Gem-Finder
# Used as a database to search for movies, insert movies, delete movies, update times, join query, aggregate query and utilizes a hidden gems feature to identify movies with low amounts of ratings yet high ratings. 
# Link to datasets: https://datasets.imdbws.com/    The ones utilized are name.basic, titles.basic, titles.principals, and titles.ratings. To use, import files to the respective one on the sql, run the commoands on the txt file, run the backend and then the frontend
